# web-dev-4-quiz-game
This is an interactive console-based quiz game built using JavaScript. It runs entirely in the browser console, asks multiple questions, gives immediate feedback, and tracks the player's score. This project demonstrates core JavaScript concepts such as arrays, objects, loops, functions, string manipulation, and conditionals.
# Features
-uses an array of objects to store questions and answers.
-Prompts the user for input using prompt() and provides feedback using alert().
-Automatically normalizes user input with toLowerCase() and trim() for accurate comparisons.
-Provides instant feedback for correct and incorrect answers.
-Tracks score and percentage at the end of the quiz.
-Displays performance messages based on results.
-Includes a restart option to retry the quiz.
-Simple, clean, and fully console-based—no HTML or DOM required.
# How to Run
1)Open a modern browser (Chrome, Firefox, Edge, etc.).  
2)Open the console:
   a) Windows/Linux: Ctrl + Shift + J
  b) Mac: Cmd + Option + J
3)Copy the content of quiz.js and paste it into the console.
4)Press Enter to start the quiz.
5)Answer each question when prompted.
6)View your score, percentage, and performance message at the end.
7)Choose whether to restart the quiz when prompted.
# Quiz Questions
Javascript-Quiz/ ├─ quiz.js ← Main JavaScript quiz code |--hello.html ├─ README.md ← Project description and instructions
# Author
Aditya Dangi
